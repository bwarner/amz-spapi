# Force npm package manager (disable yarn auto-detection)
package-manager=npm

# Disable automatic yarn detection
enable-yarn=false

# Use npm registry
registry=https://registry.npmjs.org/

# Optimize performance
legacy-peer-deps=true
audit=false
fund=false
